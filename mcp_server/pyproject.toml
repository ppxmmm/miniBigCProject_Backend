[project]
name = "minibigc-mcp-server"
version = "0.1.0"
description = "MCP server that exposes the Mini BigC backend API as AI-callable tools."
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0.0",
]

[project.scripts]
minibigc-mcp-server = "server:main"
